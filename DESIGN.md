# Aurora Suite 品牌界面规范

## 视觉方向

Aurora Suite 面向单店餐饮的扫码点单和手机运营。界面以安静的纸面、门店酒红和真实菜品图片建立识别度；点餐首页允许沉浸式迎宾，订单、表单和管理页面保持紧凑、清晰、可单手操作。

## 令牌

| 角色 | 值 | 用途 |
| --- | --- | --- |
| 品牌主色 | `#8c271e` | 价格、选中、主要按钮、图标和角标 |
| 品牌按压色 | `#6f1f19` | 主按钮按压和深色强调 |
| 品牌浅底 | `#f7ebe8` | 轻选中、图标底座和提示 |
| 页面背景 | `#f9f9f9` | 页面和滚动区域 |
| 主表面 | `#ffffff` | 信息卡、列表、固定栏和弹层 |
| 主文字 | `#1a1c1c` | 标题、正文和主要数值 |
| 次文字 | `#656461` | 说明、次级标签和桌码信息 |
| 弱文字 | `#9ca3af` | 未选中导航、占位和弱提示 |
| 分割线 | `#ebe8e6` | 列表和表单边界 |
| 成功 | `#287b61` | 成功、在线和已完成状态，配合文案 |
| 警告 | `#a66a23` | 待处理、赠送和未打印状态，配合文案 |
| 错误 | `#b3261e` | 错误和危险操作，配合文案 |

令牌位于 `miniprogram/app.wxss` 的 `page` 变量中。页面级样式只在需要表达业务状态或尺寸时覆盖，第三方 Vant/ColorUI 源码不修改。

## 字体与形状

- 正文和控件使用系统无衬线字体栈；迎宾品牌名、迎宾标题、信息卡主值和重点价格使用系统衬线回退。
- 迎宾信息卡圆角为 `56rpx`，菜单主面顶部圆角为 `32rpx`，顾客菜品卡约 `32rpx`，管理列表项约 `16rpx`。
- 输入框和主要按钮保持 `24rpx` / `26rpx` 圆角；固定操作栏使用 `40rpx` 圆角。
- 所有操作热区保留至少 `88rpx × 88rpx`，图标在稳定尺寸盒内居中。

## 迎宾动效

迎宾和菜单是 `pages/index/index` 的同一页面。`welcome-motion.wxs` 在视图层响应右侧菜品列表的滚动和触摸事件，逻辑层只接收低频状态提交。

- 上滑收起距离 `190px`，下拉恢复距离 `108px`，吸附阈值 `0.45`。
- 展开态按设备高度插值卡片和菜单位置；标准 390×844 与 430×844 对齐原型，320×568 保证菜单标题和首个菜品仍可见。
- 收起态恢复项目最初的紧凑横卡：胶囊底部下方 `8px`、左右约 `10px` 留白、约 `60px` 高和 `12px` 圆角；菜单在卡片底部下方约 `10px` 承接，顶部背景颜色不作为该形态的必要条件。
- 进度到 `1` 后锁存为收起态；回到列表顶部不会自动展开，只有继续下拉才进入恢复。
- 收起态点击紧凑门店卡可直接展开迎宾区；若菜单已滚动，展开期间保留实际滚动位置和当前分类，下一次收起后恢复原菜单上下文，不得误记为列表顶部。
- 提示使用本地图标的双 `chevron-up`，周期 `1s`，展开早期淡出，收起后隐藏。
- 购物车抽屉、规格弹层和授权弹层打开时冻结迎宾进度并隐藏底部 TabBar 的交互；关闭后恢复原状态。

## 导航和页面边界

自定义 TabBar 仅保留点餐、充值、订单、我的四个真实入口，分别使用原型同款的 Font Awesome `utensils`、`credit-card`、`file-lines` 和 `user` 本地图标，白色高不透明背景和酒红选中态；购物车、充值确认栏和提交栏均为 TabBar 与底部安全区预留空间。

Tab 切换使用普通/选中图标双层交叉淡化、约 `420ms` 的短促落位回弹、文字同步上浮和一次性低透明扩散。动效只发生在稳定图标盒内部，不改变导航尺寸；快速点击期间串行切换，系统减少动态效果时停用回弹和扩散。

非 Tab 页面复用 `components/aurora-navbar`，返回触控区与原生胶囊对齐。管理员端沿用相同颜色、字体和控件令牌，但不复制迎宾大图和装饰性结构。

## 状态规则

加载、空、错误、未授权、禁用、提交中、成功、失败和图片失败均使用当前页面的稳定占位与明确文案。演示数据使用非个人化测试内容；未授权状态不伪造头像、昵称或完整手机号。真实支付、云数据和打印结果只有在有效测试环境下才可宣称成功。

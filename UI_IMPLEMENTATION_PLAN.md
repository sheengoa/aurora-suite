# Aurora Suite 前端实施计划

## 1. 目标与基线

本计划以已确认的高保真原型 `/tmp/aurora-suite-ui-proposal/index.html` 为视觉基线，将现有微信小程序统一为适合单店餐饮高频使用的界面。

实施目标：

- 保留现有页面路由、云函数、数据结构、支付逻辑和管理员权限逻辑。
- 顾客端与管理员端共享同一套红色品牌、圆角按钮、图标、表单和状态规范。
- 点餐、充值、订单、个人中心、结算和管理员操作均可单手完成，固定操作区避让底部导航与安全区。
- 使用自定义底部导航实现精细图标、选中标记和短促切换动效。
- 未授权时保持明确的未授权身份状态，不伪造昵称、头像或手机号。

不在本轮范围：

- 修改业务接口、支付流程、数据库结构或云函数协议。
- 增加多门店、营销中心、统计报表等新业务能力。
- 重写现有管理功能或改变菜单入口的实际去向。

## 2. 视觉与交互规范

### 2.1 设计令牌

| 类型 | 规范 |
| --- | --- |
| 品牌色 | `#8c271e` |
| 品牌深色 | `#6f1f19` |
| 品牌浅底 | `#f7ebe8` / `#fcf5f3` |
| 页面背景 | `#f9f9f9` |
| 主文字 | `#1a1c1c` |
| 次文字 | `#656461` |
| 弱文字 | `#9ca3af` |
| 分割线 | `#ebe8e6` |
| 辅助绿色 | `#287b61` |
| 辅助黄色 | `#a66a23` |
| 小圆角 | `12rpx` |
| 常规圆角 | `16rpx` |
| 按钮圆角 | `26rpx`，高度 `88rpx` 至 `92rpx` |
| 浮动操作栏圆角 | `40rpx` 至 `60rpx`，按参考栏形态确定 |
| 页面阴影 | `0 12rpx 34rpx rgba(27, 36, 48, 0.07)` |
| 品牌按钮阴影 | `0 10rpx 22rpx rgba(185, 52, 43, 0.24)` |

### 2.2 控件规则

- 主按钮：红色实底、白字、稳定高度、文字与图标双轴居中，按压缩放至 `0.97`。
- 次按钮：白底或浅灰底，使用边框和文字区分，不依赖颜色 alone 表达状态。
- 图标按钮：可见图标可小于 `44px`，但触控热区不得小于 `88rpx × 88rpx`。
- 输入框：最小高度 `88rpx`，浅灰背景、细边框、`12rpx` 圆角；键盘出现时当前字段和提交按钮仍可操作。
- 弹层：遮罩期间禁止背景点击与滚动；标题、内容、底部操作区分层清晰。
- 卡片：仅用于列表项、业务对象和弹层，不嵌套卡片；常规圆角不超过 `16rpx`。
- 动效：导航切换 `180–220ms`，按钮按压 `120–160ms`，不使用大幅位移或循环装饰动效。

## 3. 页面与原型映射

| 正式页面 | 原型基线 | 实施重点 |
| --- | --- | --- |
| `pages/index/index` | `#order` | 店铺头部、分类导航、菜品列表、数量操作、浮动购物车、购物车抽屉 |
| `pages/recharge/recharge` | `#recharge` | 储值方案、选中状态、与点餐一致的浮动支付栏 |
| `pages/myorder/myorder` | `#orders` | 分段筛选、订单层级、充值/点餐订单差异、空状态 |
| `pages/myhome/myhome` | `#profile` | 未授权身份、余额、常用入口、管理员入口 |
| `pages/settle/settle` | `#order` 的结算延伸 | 堂食/打包、桌码、备注、明细、支付方式、固定提交栏 |
| `pages/dish-detail/dish-detail` | 点餐详情弹层规范 | 图片、规格、标签、数量、分享、购买固定操作区 |
| `pages/admin/admin` | `#admin` | 胶囊对齐导航、运营摘要、两列功能入口、图标居中 |
| 管理员子页 | `#system` 与 `#admin` | 工具栏、列表、表单、状态、弹层和危险操作统一 |

## 4. 实施阶段

### 阶段 A：公共基础与底部导航

涉及文件：

- `miniprogram/app.json`
- `miniprogram/app.wxss`
- `miniprogram/custom-tab-bar/index.{json,wxml,wxss,js}`
- `pages/index/index.js`
- `pages/recharge/recharge.js`
- `pages/myorder/myorder.js`
- `pages/myhome/myhome.js`

任务：

- 补齐公共颜色、圆角、阴影、按钮、按压和安全区令牌。
- 启用微信 `custom-tab-bar`。
- 使用同一 ColorUI 图标族重写点餐、充值、订单、我的四个图标。
- 增加滑动选中标记、图标盒选中态和按压反馈。
- 四个 Tab 页面在 `onShow` 同步正确选中索引。

验收：

- 四个入口均可点击并跳转到正确页面。
- 冷启动、返回和快速切换后选中项正确，无上一页状态残留。
- 导航不遮挡内容或浮动操作栏，底部手势区留白正确。

### 阶段 B：顾客端主流程

涉及文件：

- `pages/index/index.{wxml,wxss}`
- `pages/recharge/recharge.{wxml,wxss}`
- `pages/myorder/myorder.{wxml,wxss}`
- `pages/myhome/myhome.{wxml,wxss}`
- `pages/settle/settle.{wxml,wxss}`
- `pages/dish-detail/dish-detail.{wxml,wxss}`

任务：

- 对齐原型的页面结构、信息密度、按钮曲率和品牌层级。
- 点餐与充值固定栏使用同一外框、金额摘要和操作按钮比例。
- 所有固定栏为滚动内容预留准确空间。
- 统一分段控件、数量步进、支付方式、空状态和错误状态。
- 保持未授权个人中心，不展示仿真身份信息。

验收：

- 点餐、查看详情、选规格、加入购物车、结算、充值、查订单可连续操作。
- 最长菜名、金额、标签、备注和无图片状态不溢出。
- 小屏和大屏下无横向滚动、遮挡或布局跳变。

### 阶段 C：管理员首页与子页

涉及文件：

- `pages/admin/admin.{wxml,wxss}`
- `pages/admin/dish/dish.{wxml,wxss}`
- `pages/admin/dishCategory/dishCategory.{wxml,wxss}`
- `pages/admin/user/user.{wxml,wxss}`
- `pages/admin/order/order.{wxml,wxss}`
- `pages/admin/shopSettings/shopSettings.{wxml,wxss}`
- `pages/admin/rechargeOptions/rechargeOptions.{wxml,wxss}`
- `pages/admin/tableCode/tableCode.{wxml,wxss}`
- `pages/admin/printer/printer.{wxml,wxss}`

任务：

- 管理员首页按原型实现两列入口网格，图标在色块中双轴居中。
- 保留菜品、会员、订单、桌码、店铺、充值、打印机和账号安全的原事件与路由。
- 子页统一工具栏、搜索、状态标签、列表项、表单、开关、弹层和危险操作。
- 删除仅由本轮替换产生的无用样式，不改动无关业务代码。

验收：

- 每个入口均有按压反馈并进入正确页面。
- 新建、编辑、删除、启停、搜索、刷新、扫码、预览、下载和弹层关闭流程可用。
- 表单在键盘、校验失败、提交中和提交完成状态下保持可操作。

### 阶段 D：验证与收尾

检查项：

- `git diff --check`、JSON 解析、WXML 标签闭合和关键事件绑定检查。
- 使用 `speedcode-skill` 连接微信开发者工具，逐页真实渲染截图。
- 在至少一个小屏和一个大屏模拟器尺寸检查顾客端与管理员端。
- 操作底部导航、返回、按钮、弹层、输入、滚动和固定栏。
- 检查正常、未授权、空、加载、错误、成功、禁用、图片失败和最长文案状态。
- 冷启动、热启动、首次进入、重复进入、前进、返回、快速切换和刷新无跨页闪现。

完成标准：

- 所有计划页面与原型视觉语言一致。
- 所有既有业务入口与事件保持可用。
- 所有真实渲染截图人工检查无可见问题。
- 没有未验证项时才可判定“全部检查通过”。

## 5. 执行记录

| 阶段 | 状态 | 结果 |
| --- | --- | --- |
| A 公共基础与底部导航 | 已完成 | 已完成自定义图标、选中标记、按压反馈和页面级索引同步；在微信开发者工具真实模拟器确认点餐、充值、订单、我的四个页面分别高亮正确。 |
| B 顾客端主流程 | 已完成（临时环境） | 已完成并核验点餐、充值、订单、个人中心、结算、菜品详情的结构、圆角按钮、固定操作栏、空态和错误态；真实运行态验证分类、加购/减购、购物车抽屉、规格选择、充值方案切换和订单筛选。真实云环境数据态与支付回调不属于本地模拟验收。 |
| C 管理员首页与子页 | 已完成（临时环境） | 管理员首页与菜品、分类、会员、订单、店铺设置、充值方案、桌码、打印机 8 个子页均已真实截图检查；代表性新增/编辑/删除/启停/搜索/刷新/桌码成功态/打印机弹层已覆盖。云端持久化结果需在有效环境重跑。 |
| D 验证与收尾 | 已完成（临时环境） | `speedcode-skill` 已完成 15 路由截图、迎宾动效关键帧、规格/购物车/Tab 锁定专项、320×568 与 430×932 尺寸检查；已清理旧亮红和装饰斜纹，完成 JS/JSON/WXS 静态检查并归档至 `docs/visual-qa/2026-08-09/brand-ui-final/`。 |

## 6. 本轮验证记录

真实微信开发者工具验证副本：`/tmp/aurora-suite-frontend-test.giSCPz`，IDE 服务端口 `29451`，本轮小程序自动化端口 `29460`。

已人工打开并检查的截图目录：

- 全量正常数据态：`docs/visual-qa/2026-08-07/full-normal/`
- 弹层与操作态：`docs/visual-qa/2026-08-07/open-states/`
- 充值选中方案原型对照：`/tmp/devtools-recharge-selected-real-window.png`
- 本轮运行时假数据：`docs/visual-qa/2026-08-07/`

本轮假数据仅通过 `miniProgram.evaluate` 在运行中的页面调用 `Page.setData` 注入，使用“未授权用户”文案，不展示仿真个人身份信息，未写入项目源码或云数据库。归档截图包括 15 个页面正常数据态、9 个弹层操作态，以及订单全部/已完成筛选、充值默认/切换 ¥300 方案四张补充截图。

本轮还修复了公共页面初始化拦截：云初始化失败时仍调用页面自身 `onLoad`，使无参数详情页能够显示“没有找到这道菜”的错误态，而不是无限停留在骨架屏。

顶部返回导航专项以 `/home/ao/project/mini-program/industry-showroom/src/components/experience/experience-navbar.vue` 为规格基线，新增统一 `aurora-navbar` 组件并覆盖菜品详情、订单结算、管理员首页和 8 个管理员子页。微信开发者工具实测外层触控区为 `76px × 44px`，内部胶囊为 `42px × 32px`，左边距为 `9px`；管理员子页返回管理员首页、菜品详情返回点餐页两条链路均通过模板绑定的 `handleBack` 验证。

当前未通过项：验证副本的 `env` 与 AppID/云函数配置不完整，无法真实拉取菜品、充值方案、订单和管理员业务数据；本轮数据态仅用于视觉与交互模拟，支付提交、真实订单查询和依赖后端的管理操作必须在配置有效环境后重新执行。

## 7. 2026-08-10 最终复验

本轮在临时 AppID 与本地模拟数据范围内完成以下补充修复和复验：

- 订单详情、个人信息授权、管理员密码和充值授权弹层打开时锁定自定义 TabBar，关闭后恢复，避免底部导航穿透、抢占点击或覆盖弹层操作区。
- 超长订单详情的底部操作栏使用不透明白底和独立层级，10 个商品、长规格、长备注滚动时不再透出正文。
- 订单筛选、管理员订单筛选、会员连续搜索和手动刷新均增加竞态保护，快速操作后只保留最后一次请求对应的页面状态。
- 手机号按 `138****5678` 形式脱敏；授权弹层重复打开、余额弹层、桌码弹层和储值方案弹层均已真实渲染检查。

最终检查结果：

- 全部业务 JavaScript 与云函数通过 `node --check`。
- 125 个项目 JSON 配置通过严格解析，依赖目录中的 JSONC 配置不纳入业务 JSON 统计。
- `git diff --check` 通过。
- 微信开发者工具当前构建在 `430×932`、`390×844` 和 `320×568` 三种代表尺寸完成复验；最终关键页面截图位于 `/tmp/aurora-suite-vqa-20260810/final-smoke-r1/`、`/tmp/aurora-suite-vqa-20260810/final-390-r1/` 和 `/tmp/aurora-suite-vqa-20260810/final-320-r1/`。
- 全量 15 路由、弹层锁定、长订单、筛选竞态和管理弹层证据位于 `/tmp/aurora-suite-vqa-20260810/full-routes-r3/`、`/tmp/aurora-suite-vqa-20260810/tabbar-lock-interaction-r2/`、`/tmp/aurora-suite-vqa-20260810/race-long-r2/` 和 `/tmp/aurora-suite-vqa-20260810/interaction-r2/`。

本轮已确认的迎宾卡片形态、折叠惯性参数、菜单滚动锁定、欢迎区域滑动范围、菜品加减按钮以及菜品上下架配色与语义均未再改动。

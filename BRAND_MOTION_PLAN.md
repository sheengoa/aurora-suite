# Aurora Suite 全前端品牌视觉与迎宾动效实施计划

## 1. 文档控制

### 1.1 当前状态

- 计划状态：已实施并完成临时 AppID / 本地模拟数据验收。
- 目标分支：`feature/aurora-brand-ui`（当前分支）。
- 分支基线：`main` 当前提交 `4173302`。
- 产品事实基线：`PRODUCT.md`、现有小程序页面、路由、业务代码和云函数协议。
- 视觉与交互基线：`docs/design/brand-motion-proposal/index.html`。
- 原型渲染基线：`docs/design/brand-motion-proposal/index.html`。
- 最终渲染证据：`docs/visual-qa/2026-08-09/brand-ui-final/`。
- 既有页面验证记录：`UI_IMPLEMENTATION_PLAN.md` 与 `docs/visual-qa/`。

本计划替换本文件此前以“迎宾、支付成功、充值成功三段动效”为中心的旧方案。此前的支付成功动画和充值成功动画已经退出本轮范围，不得在实施时继续引用。`UI_IMPLEMENTATION_PLAN.md` 中记录的业务现状、已发现问题和验证风险继续有效，但其中旧的亮红视觉基线不再具有设计权威。

### 1.2 决策优先级

出现冲突时按以下顺序处理：

1. 用户在当前任务中明确确认的要求。
2. 已确认迎宾原型的结构、色调、比例和交互动效。
3. `PRODUCT.md` 中的产品事实、用户场景和约束。
4. 当前页面的业务功能、数据结构、路由和云函数协议。
5. 当前代码中的视觉样式，仅作为迁移事实，不作为新设计基线。

### 1.3 完成结论

最终结论只能是：

- `全部检查通过`：计划内页面、状态、控件、流程、设备尺寸和真实渲染均已验证，且没有遗留缺陷或未验证项。
- `未通过`：存在任何视觉偏差、功能回归、环境阻塞或未验证项。

不得用“基本完成”“主要流程可用”或“仅有小问题”替代上述结论。

## 2. 总体任务

在新分支中将已确认的迎宾原型完整实施到 Aurora Suite 微信小程序，并将原型建立的视觉语言扩展到全部 15 个前端路由和公共组件。

“完整实施”同时包含以下内容：

- 点餐首页的展开迎宾态、连续收起过程、紧凑菜单态和下拉恢复过程。
- 原型中的门店背景、欢迎文案、三栏信息卡、菜单交界、上滑提示、菜品列表、购物车栏和底部导航。
- 原型底部导航的外观、图标语义、选中状态、触控反馈、路由和安全区处理。
- 顾客端其余页面的完整视觉迁移。
- 管理员端全部页面、列表、工具栏、表单、弹层和状态的完整视觉迁移。
- 加载、空、错误、成功、禁用、未授权、图片失败、慢网、提交中等状态。
- 不同设备、系统字体、原生胶囊、键盘和底部安全区适配。
- 现有点餐、桌码、购物车、结算、支付、充值、订单、授权和管理员能力不回归。

这不是把原型截图覆盖到首页，也不是只替换颜色。最终结果必须是一套真实数据驱动、可交互、可恢复、可验证的完整小程序界面系统。

## 3. 范围与边界

### 3.1 页面范围

本轮覆盖 `miniprogram/app.json` 注册的全部 15 个路由：

| 分组 | 页面 | 路由 |
| --- | --- | --- |
| 顾客端 | 点餐首页 | `pages/index/index` |
| 顾客端 | 菜品详情 | `pages/dish-detail/dish-detail` |
| 顾客端 | 会员充值 | `pages/recharge/recharge` |
| 顾客端 | 我的订单 | `pages/myorder/myorder` |
| 顾客端 | 个人中心 | `pages/myhome/myhome` |
| 顾客端 | 订单结算 | `pages/settle/settle` |
| 管理端 | 管理员首页 | `pages/admin/admin` |
| 管理端 | 菜品管理 | `pages/admin/dish/dish` |
| 管理端 | 菜品分类管理 | `pages/admin/dishCategory/dishCategory` |
| 管理端 | 会员管理 | `pages/admin/user/user` |
| 管理端 | 订单管理 | `pages/admin/order/order` |
| 管理端 | 店铺设置 | `pages/admin/shopSettings/shopSettings` |
| 管理端 | 充值方案管理 | `pages/admin/rechargeOptions/rechargeOptions` |
| 管理端 | 桌码管理 | `pages/admin/tableCode/tableCode` |
| 管理端 | 打印机管理 | `pages/admin/printer/printer` |

`pages/admin/dishCategory/dishCategory` 即使当前不是主要入口，也仍是已注册路由，必须实施和验收，不得因正常流程中不容易看到而跳过。

### 3.2 公共范围

- `miniprogram/app.json`：全局窗口与自定义 Tab 配置。
- `miniprogram/app.wxss`：设计令牌和有限的公共基础样式。
- `miniprogram/custom-tab-bar/`：原型底部导航的正式实现。
- `miniprogram/components/aurora-navbar/`：统一返回导航和原生胶囊避让。
- `miniprogram/components/avatarNicknameModal/`：授权弹层视觉与交互统一。
- Vant Weapp 现有图标、标签、分段和空状态使用处。
- ColorUI 现有图标使用处，避免旧主题颜色泄漏。
- 页面图片、图标、骨架、错误占位和品牌背景资源。

### 3.3 保持不变

- 页面路由语义和入口去向。
- 云函数名称、参数、返回结构和数据库模型。
- 购物车数据结构和本地存储键。
- 菜品分类、分页、规格、标签和价格计算逻辑。
- 微信支付、余额支付和充值支付业务流程。
- 管理员会话、权限检查和隐藏入口规则。
- 桌码生成、海报预览下载和打印机业务接口。
- 现有真实文案的事实含义。

允许为视觉状态、滑动动效、生命周期清理和可访问性补充最小必要的前端状态，但不得借视觉改造重写业务层。

### 3.4 明确不做

- 不实现旧计划中的点餐成功分镜动画。
- 不实现旧计划中的充值成功分镜动画。
- 不新增多门店、优惠券、营销中心、统计报表等业务。
- 不修改数据库权限、支付回调和云函数协议。
- 不为了视觉统一大规模替换 Vant、ColorUI 或 Painter。
- 不建立通用动画框架、通用表单框架或通用弹层框架。
- 不伪造个人昵称、头像、手机号、订单或支付成功结果。

## 4. 当前仓库事实与迁移问题

### 4.1 当前事实

- 实施前基线为 `main@4173302`；当前分支已迁移至酒红令牌和原型底部导航，页面级旧亮红已清理，第三方 Vant/ColorUI 源码保持不改。
- 当前自定义 TabBar 有顶部滑动标记、图标底座和选中缩放，与已确认原型不一致。
- 当前点餐页只有静态店铺头部，没有原型中的完整展开迎宾态和连续收起动效。
- 当前页面中存在大量页面级原始颜色、圆角和阴影，不能只改 `app.wxss` 就完成统一。
- 现有 15 个正常页面截图、多个弹层截图和导航专项截图可作为改造前基线。
- 当前仓库没有自动化单元测试体系，真实渲染验证依赖微信开发者工具和 `speedcode-skill`。
- 当前 `project.config.json` 的 AppID、`miniprogram/app.js` 的云环境仍是占位值，真实支付、扫码、云数据提交和打印不能在未配置环境中宣称通过。

### 4.2 工作区保护

当前工作区包含未跟踪的 `BRAND_MOTION_PLAN.md`、`PRODUCT.md`、`docs/`、`output/` 和 `.playwright-cli/`。正式实施前必须：

1. 记录 `git status --short` 和当前提交。
2. 不执行 `git clean`、`git reset --hard` 或删除未跟踪内容。
3. 从 `main@4173302` 创建 `feature/aurora-brand-ui`。
4. 只选择性暂存本任务文件，禁止直接使用 `git add .`。
5. `.playwright-cli/` 和临时 `output/` 不进入实施提交；确认后的设计稿和 QA 证据归档到 `docs/`。

## 5. Git 分支与提交计划

### 5.1 分支创建

正式实施开始时执行：

```bash
git status --short
git branch --show-current
git switch -c feature/aurora-brand-ui
```

创建后再次确认：

```bash
git branch --show-current
git status --short
```

### 5.2 提交边界

按可独立验证的目的拆分提交：

1. `文档：锁定全前端品牌视觉实施计划`
2. `工程：建立品牌视觉令牌与公共资源`
3. `功能：实现迎宾滑动与点餐首页`
4. `功能：统一底部导航与顾客端页面`
5. `功能：统一管理员端页面`
6. `修复：补齐全页面状态与视觉验收问题`
7. `文档：归档全前端真实渲染验收结果`

每个提交前执行对应范围的静态检查和真实渲染检查。公共组件、全局样式、导航或安全区发生变化后，必须重新验证全部受影响页面，不能只检查当前页面。

## 6. 设计合同

### 6.1 视觉主张

- 门店照片和迎宾文案负责建立第一印象。
- 白色实底信息卡负责承载桌号与服务事实。
- 菜单以有明确间隙和圆角顶边的主面承接迎宾区。
- 酒红色只承担品牌、价格、选中和主要操作，不使用高饱和橙红渐变。
- 顾客端可以有餐饮氛围，管理员端保持安静、紧凑和工作导向。
- 依靠比例、留白、图片和信息层级建立质感，不堆叠装饰卡片、玻璃效果和无意义动效。

### 6.2 颜色令牌

正式实施先将 `app.wxss` 中的旧亮红体系替换为语义令牌，再逐页消除旧颜色硬编码。

| 角色 | 目标值 | 用途 |
| --- | --- | --- |
| 品牌主色 | `#8c271e` | 价格、选中、主要按钮、图标、角标 |
| 品牌按压色 | `#6f1f19` | 主按钮按压和深色强调 |
| 品牌浅底 | `rgba(140, 39, 30, 0.07)` | 图标底座、轻选中背景 |
| 页面背景 | `#f9f9f9` | 顾客端和大部分管理端页面底色 |
| 主表面 | `#ffffff` | 信息卡、列表项、弹层、固定栏 |
| 主文字 | `#1a1c1c` | 标题、正文、主要数值 |
| 次文字 | `#656461` | 辅助说明、桌号次级信息 |
| 弱文字 | `#9ca3af` | 未选中导航、占位和弱提示 |
| 分割线 | `#edecea` | 列表和表单边界 |
| 成功色 | `#2f7d55` | 成功与在线状态，同时配文案或图标 |
| 警告色 | `#a66a16` | 警告与待处理状态，同时配文案或图标 |
| 错误色 | `#b3261e` | 错误与危险操作，同时配文案或图标 |

要求：

- 购物车图标、数量角标、价格和结算按钮必须是同一 `#8c271e`。
- 删除旧的 `#d94335 -> #f36a5d` 橙红渐变在主要操作中的使用。
- 不使用毛玻璃迎宾卡或毛玻璃购物车栏。
- TabBar 可以保留原型中的轻透明感，但必须有高不透明白色降级背景，不能依赖模糊才能读清。
- 状态不能仅靠颜色表达，必须同时有文字、图标、形状或位置差异。

### 6.3 字体与信息层级

- 正文、按钮、表单和管理员页面使用系统无衬线字体栈，保证微信端稳定和清晰。
- `Aurora` 品牌名、迎宾标题、信息卡主值、菜单标题和重点价格使用原型中的衬线角色；使用系统 `serif`/宋体回退，不引入运行时 Google Fonts 依赖。
- 动态门店名称必须在系统字体回退下仍保持稳定尺寸，不因字体加载造成跳动。
- 品牌展示字号仅用于迎宾区，紧凑卡、面板、列表和管理工具栏使用更小更紧的层级。
- 除迎宾 kicker、英文菜单副标题和 Tab 标签外，`letter-spacing` 固定为 `0`。
- 页面标题不添加标点，最长真实文案必须明确设计截断、换行或自适应规则。

### 6.4 圆角、阴影与间距

| 角色 | 目标值 |
| --- | --- |
| 基础间距 | `8rpx` 倍数体系 |
| 输入框圆角 | `24rpx` |
| 主要按钮圆角 | `26rpx` |
| 管理端列表项圆角 | `16rpx` |
| 顾客端菜品卡圆角 | `32rpx` |
| 图片圆角 | `24rpx` |
| 浮动操作栏圆角 | `40rpx` |
| 迎宾信息卡圆角 | `56rpx` |
| 菜单顶部圆角 | `32rpx 32rpx 0 0` |
| 圆形加减按钮 | `50%` |

- 卡片仅用于单个业务对象、弹层和真正需要框定的工具，不做卡片套卡片。
- 页面区段采用无框带状布局，不把每个区段都做成浮动卡片。
- 常规阴影保持中性和低对比；品牌红阴影只用于主要操作且面积受控。
- 卡片、图片、工具栏和按钮尺寸必须稳定，加载、数量变化和状态切换不能引发布局跳动。

### 6.5 图标和图片

- 底部导航使用与原型相同语义和轮廓的四个本地图标：餐具、银行卡、订单文件、用户。
- 原型图标不得用含义相近但轮廓明显不同的图标替代；输出普通态和选中态本地资源，避免运行时依赖 Font Awesome CDN。
- “向上浏览菜单”使用原型同款双 `chevron-up` 资源、尺寸、间距和 `bounce` 节奏。
- 购物车继续使用项目 `miniprogram/images/shopping.png` 的轮廓，并统一着色为 `#8c271e`；若 WXSS 蒙版在代表性设备上不稳定，则输出同轮廓的本地酒红资源作为降级，不改图形。
- 欢迎背景不得继续热链原型中的 Unsplash 地址。实施时保存经确认、许可明确、压缩后的本地或自有云存储资源，并提供图片加载失败时的深色稳定背景。
- 菜品、头像、二维码和海报都必须有固定尺寸、加载中和失败占位。

### 6.6 控件与反馈

- 所有可点击控件的真实热区不小于 `88rpx x 88rpx`。
- 图标可小于热区，但必须置于稳定图标盒中双轴居中。
- 主按钮使用酒红实底，不使用橙红渐变；按压反馈为短促缩放和透明度变化，不改变布局。
- 次按钮使用白底、浅灰底或细边框，不以另一个高饱和颜色争夺层级。
- 数量步进器、分段控件、开关、单选和标签必须有独立于颜色的选中标识。
- 危险操作与主要操作不得并列成相同权重。
- 弹层打开时锁定背景滚动和点击，关闭后清理遮罩、键盘、监听器和临时状态。

## 7. 点餐首页与迎宾动效合同

### 7.1 页面不是两个页面

迎宾态和菜单态属于 `pages/index/index` 同一个页面。禁止使用以下替代方案：

- 新增一个独立迎宾页后再跳转点餐页。
- 用定时器自动收起迎宾区。
- 上滑达到阈值后突然切换两个静态布局。
- 只播放一次不跟手的 CSS 动画。
- 只移动菜单而不处理信息卡、欢迎文案和提示。

### 7.2 关键视觉状态

| 状态 | 视觉与交互 |
| --- | --- |
| `expanded` | 完整背景、欢迎文案、三栏白色信息卡、菜单圆角顶边和上滑提示可见；首屏必须露出菜单标题与首个菜品的一部分。 |
| `collapsing` | 进度由右侧菜品列表上滑距离连续驱动；欢迎文案淡出，信息卡从底部收缩并上移，三栏内容淡出，紧凑内容淡入，菜单同步上移。 |
| `collapsed` | 信息卡成为顶部紧凑门店条，菜单从其下方占据主要视口，上滑提示隐藏，菜品列表继续正常滚动。 |
| `expanding` | 菜品列表已经回到顶部后，用户继续下拉，紧凑条、菜单、欢迎文案和背景按反向进度连续恢复。 |

430 x 844 原型基线坐标：

- 展开态 header：`0 - 291.75px`。
- 展开态信息卡：`347.75 - 444.75px`。
- 展开态菜单顶部：`451.75px`。
- 信息卡与菜单之间保持约 `7px` 的明确间隙，不能相贴或互相遮盖。
- 展开态上滑提示：`465.75 - 505.25px`。
- 收起态信息卡：位于原生胶囊底部下方 `8px`，左右留白约 `10px`，高度约 `60px`，圆角约 `12px`；采用项目最初的紧凑横向门店卡，不使用大圆角悬浮胶囊。
- 收起态菜单顶部：信息卡底部下方约 `10px`，随胶囊安全位置动态计算。

实际小程序必须以原生状态栏和胶囊位置为安全基准进行动态换算；在相同 430 x 844 页面状态下，与原型逐项对照结构和相对比例，不把上述坐标机械套用到所有设备。

### 7.3 进度与吸附规则

保持原型已确认的行为参数：

- 上滑收起距离：`190px`。
- 下拉恢复距离：`108px`。
- 收起进度：`clamp(scrollTop / 190, 0, 1)`。
- 收起锁存：进度到 `1` 后进入 `collapsedLatch`，列表后续滚动不再修改迎宾进度。
- 恢复起点：仅当已锁存且右侧菜品列表 `scrollTop === 0` 时接受向下拉动。
- 结束吸附：下拉结束后进度小于 `0.45` 则展开到 `0`，否则回到收起态 `1`。
- 收起判定视觉阈值：进度大于 `0.92` 进入紧凑状态类。
- 上滑提示在收起早期淡出，进入菜单态后完全隐藏。
- 双箭头 `bounce` 周期为 `1s`，0%/100% 上移 25%，50% 回到原位，缓动与原型一致。

阈值作为原型合同保存，不在实施过程中凭手感反复调整。只有真实设备测试证明触控距离在特定尺寸上不可用时，才按等比例规则调整，并重新取得确认。

### 7.4 视图层实现

当前项目使用 WebView 渲染，`project.config.json` 未启用 Worklet 编译。本轮采用页面内单用途 WXS 文件实现高频交互，不迁移 Skyline，也不使用高频 `setData` 驱动每一帧。

预计新增：

- `miniprogram/pages/index/welcome-motion.wxs`

职责边界：

- WXS 在视图层响应 `scroll-view`、`touchstart`、`touchmove` 和 `touchend`。
- WXS 使用 `ComponentDescriptor.setStyle` / `setClass` 更新 `.welcome-scene` 内部节点。
- 位移、透明度、裁切和必要高度变化直接在视图层完成。
- 页面 JS 只接收展开、收起、恢复完成等低频语义事件，保存稳定状态。
- 现有分类同步逻辑通过 WXS 节流后调用页面方法，不能每帧跨线程通信。
- `scrolltolower` 继续独立承担分页加载，不与迎宾进度耦合。
- 不建立跨页面通用动效引擎。

技术依据：微信官方 [WXS 响应事件](https://developers.weixin.qq.com/miniprogram/dev/framework/view/interactive-animation.html) 明确用于避免频繁交互中视图层与逻辑层往返及 `setData` 延迟；`scroll-view` 行为以微信官方 [scroll-view 文档](https://developers.weixin.qq.com/miniprogram/dev/component/scroll-view.html) 为准。

### 7.5 滚动和手势归属

- 右侧菜品列表是唯一迎宾进度驱动源。
- 左侧分类列表滚动不改变迎宾状态。
- 点击分类触发程序滚动时，先保持当前迎宾稳定状态，再更新右侧列表和分类选中项。
- 收起锁存后，菜品列表继续滚动；滚回顶部本身不自动展开，必须有明确下拉手势。
- 购物车抽屉、菜品规格弹层、授权弹层打开时冻结迎宾进度并锁定背景。
- 弹层关闭后按打开前状态恢复，不从中间跳到另一状态。
- 原生下拉刷新和迎宾下拉恢复必须在技术验证阶段确定优先级：收起态首次下拉先恢复迎宾；完全展开后的下一次下拉才允许刷新。
- 如果页面级 `enablePullDownRefresh` 与内层 `scroll-view` 冲突，则将刷新迁移为列表内显式 refresher，但不得取消刷新能力。

### 7.6 会话和返回行为

- 每个小程序会话首次进入点餐页默认展示完整展开态。
- 在同一会话内切换 Tab、进入菜品详情或结算再返回时，恢复离开前的迎宾状态、分类和菜品滚动位置。
- 完全退出小程序并重新启动后，再次展示完整展开态。
- 热启动不得先闪现默认状态再恢复旧状态。
- 页面隐藏时停止上滑提示循环和不必要监听；显示时按稳定状态决定是否恢复。
- 页面卸载时清理计时器、WXS 同步状态和临时手势标志。

### 7.7 性能和降级

- 动效优先使用 `transform`、`opacity` 和裁切，避免在每一帧修改阴影、模糊和复杂渐变。
- 欢迎背景保持静态，不做视差或大面积滤镜动画。
- 开始正式首页实现前先制作最小技术验证页，在真实开发者工具中验证 WXS、右侧列表滚动、分类同步、分页和下拉刷新不会互相阻塞。
- 代表性设备录屏逐帧检查，不得出现白帧、闪动、卡片穿透、列表回弹错位或滚动丢失。
- 不支持 WXS 交互动画的低版本使用稳定的展开/收起两态和短过渡，业务内容始终可见。
- 素材失败时展示深色背景和完整文字，不允许迎宾区空白或高度塌陷。

## 8. 首页其他区域实施要求

### 8.1 菜单交界

- 菜单顶部使用 `32rpx` 圆角，不能出现直角顶边。
- 信息卡和菜单之间保留原型可见间隙。
- 菜单背景、卡片白底和外层页面背景使用不同但克制的明度层级，避免边界消失。
- 菜单主面上移时圆角、阴影和间隙连续变化，不突然切换。

### 8.2 分类与菜品

- 保留左侧分类、右侧菜品和当前分类同步逻辑。
- 分类选中只使用酒红文字和字重，不增加与原型不一致的大色块。
- 菜品卡采用白底、稳定图片比例、两行描述和底部价格操作区。
- 加号图标保持小而清晰，但点击热区扩展至至少 `88rpx`。
- 无图片、图片加载中和图片失败使用同尺寸占位。
- 规格菜品仍进入详情或规格选择流程，不能因视觉统一绕过选择规则。

### 8.3 购物车栏

- 使用项目真实结构：购物图标、数量角标、总价、桌号和“去结算”。
- 外层约 `40rpx` 圆角、白色实底、中性阴影。
- 图标底座约 `30rpx` 圆角，使用品牌浅底。
- 结算按钮约 `26rpx` 圆角、`88rpx` 高、酒红实底。
- 购物车为空、无桌码、数量三位数、长价格和禁用状态都必须有明确布局。
- 点击左侧打开真实购物车抽屉，点击结算进入真实结算页。
- 购物车栏和 TabBar 之间保持原型约 `32rpx` 间隙，并为安全区准确预留内容空间。

### 8.4 购物车抽屉与规格弹层

- 统一遮罩、顶部把手、标题、滚动内容和底部操作区。
- 抽屉打开时 TabBar 和背景菜单不得响应点击。
- 内容区为固定操作栏预留空间，最后一项不能被遮挡。
- 清空、加减、规格标签、合计和结算必须保留现有功能。
- 关闭后恢复菜单滚动位置和迎宾状态。

## 9. 底部导航实施合同

### 9.1 视觉结构

完全以原型 `.reference-tabbar` 为基线：

- 四个等宽入口：点餐、充值、订单、我的。
- 导航内容高度约 `120rpx`，再叠加 `env(safe-area-inset-bottom)`。
- 顶部内边距约 `24rpx`，水平内边距在 430px 基线约 `80rpx`，小屏约 `56rpx`。
- 图标视觉尺寸约 `36rpx`。
- 图标与文案间距约 `12rpx`。
- 文案约 `18rpx`，保持单行。
- 选中为 `#8c271e`，未选中为 `#9ca3af`。
- 白色高不透明背景、极淡顶边和上方中性阴影。

必须删除当前 TabBar 中与原型不一致的：

- 顶部滑动标记。
- 选中图标底座色块。
- 选中图标上移和放大。
- 亮红 `#d94335` 选中色。

### 9.2 功能要求

- 入口使用本地资源，不能运行时依赖外部图标 CDN。
- 每个入口真实触控区不小于 `88rpx x 88rpx`。
- 点击当前 Tab 不重复跳转，也不能丢失当前页面状态。
- 快速切换、冷启动、热启动、返回和页面重建后选中项都正确。
- 每个 Tab 页 `onShow` 与自定义 TabBar 的自动路由识别保持一致，避免双源竞争。
- 按压反馈不得产生系统默认方块遮罩。
- 四个 Tab 页的滚动内容必须统一预留 TabBar 和安全区高度。
- 点餐页购物车栏位于 TabBar 上方；充值页固定充值栏同样不能遮挡导航。

## 10. 全页面迁移矩阵

### 10.1 顾客端

| 页面 | 必须实施的结构 | 必须覆盖的状态与交互 |
| --- | --- | --- |
| `pages/index/index` | 完整迎宾区、信息卡、菜单交界、分类、菜品卡、购物车栏、购物车抽屉、规格弹层、底部导航 | 展开、半收起、收起、下拉恢复、分类跳转、分页、刷新、正常、空、加载、错误、无图、无桌码、购物车空/有数据、弹层打开/关闭 |
| `pages/dish-detail/dish-detail` | 统一返回导航、图片轮播、名称价格、规格、口味标签、数量步进、分享、立即购买、加入购物车固定栏 | 骨架、加载失败、菜品不存在、0/1/9 张图片、无可售规格、必选未选、数量边界、图片预览、分享、扫码桌码、加入购物车、立即购买 |
| `pages/recharge/recharge` | 品牌化余额区、充值方案、赠送信息、储值须知、固定确认栏、底部导航 | 未授权、授权弹层、余额加载、方案加载、空、错误、分页、默认选中、切换方案、无赠送/有赠送、支付确认、取消、失败、成功返回 |
| `pages/myorder/myorder` | 分段筛选、点餐/充值订单卡、状态、商品摘要、金额、详情操作、底部导航 | 全部/已完成筛选、点餐订单、充值订单、分页、空、加载、错误、可取消/不可取消、微信退款提示、详情弹窗、取消确认/成功/失败 |
| `pages/myhome/myhome` | 未授权/已授权身份区、余额与订单入口、常用服务、联系客服、版本号、管理员入口、底部导航 | 未授权、已授权无手机号、完整资料、头像失败、授权弹层、联系客服、管理员五次触发、首次设密码、登录、密码错误、键盘弹出 |
| `pages/settle/settle` | 统一返回导航、堂食/打包分段、桌码、备注、订单明细、支付方式、固定提交栏 | 购物车空、堂食/打包、未绑定/已绑定/换桌、扫码取消/失败、长备注、无图、余额不足、微信/余额支付、未授权、提交中、支付取消/失败/成功 |

顾客端扩展原则：保留迎宾原型的酒红、白色实底、菜单纸面和克制阴影；只在点餐首页使用沉浸式门店照片，不把营销式大背景复制到订单、表单和个人中心。

### 10.2 管理员端

| 页面 | 必须实施的结构 | 必须覆盖的状态与交互 |
| --- | --- | --- |
| `pages/admin/admin` | 品牌导航、工作台标题、数据管理和系统设置入口、返回、修改密码弹层 | 会话有效/失效、8 个入口、按压反馈、返回、旧密码错误、新密码不一致、提交中、修改成功/失败 |
| `pages/admin/dish/dish` | 分类侧栏、菜品工作区、工具栏、菜品列表、分类面板、菜品大弹层、规格、图片、口味/做法嵌套弹层 | 无分类、无菜品、首屏加载、分页加载/结束、上下架、新增/编辑/删除分类、新增/编辑/删除菜品、0/1/9 图、规格增删启停、标签增删、校验和上传失败 |
| `pages/admin/dishCategory/dishCategory` | 独立分类列表、添加入口、编辑弹层 | 正常、空、加载失败、新增、编辑、排序、图标、删除确认、保存中和校验失败 |
| `pages/admin/user/user` | 搜索栏、会员列表、头像身份、余额、修改余额弹层 | 正常、空、加载、分页、搜索有结果/无结果、清空、无头像、无昵称、无手机号、长金额、编辑校验、保存成功/失败 |
| `pages/admin/order/order` | 类型筛选、刷新提示、订单列表、用户信息、点餐/充值差异、状态与商品明细 | 全部/充值/点餐、自动刷新、手动刷新、空、加载、错误、分页、未授权身份信息、长订单号、多商品、多标签、删除入口若重新启用时的危险态 |
| `pages/admin/shopSettings/shopSettings` | 顾客端迎宾预览、门店名称、欢迎词、字数计数、固定保存栏 | 初始加载、20 字店名、48 字欢迎词、空值、键盘、保存中、成功、失败；预览必须与正式迎宾组件使用同一视觉令牌 |
| `pages/admin/rechargeOptions/rechargeOptions` | 工具栏、充值方案列表、金额和赠送层级、推荐/启用状态、编辑弹层 | 正常、空、加载、分页、新增、编辑、删除、无赠送/有赠送、推荐、启用/停用、金额校验、开关、保存成功/失败 |
| `pages/admin/tableCode/tableCode` | 工具栏、二维码列表、生成状态、预览/下载/删除、新建弹层、Painter 隐藏画布 | 空、加载、生成中、二维码失败、海报可用、预览、下载权限允许/拒绝、删除确认、重复桌码、长桌号、新建成功/失败、Painter 生成失败 |
| `pages/admin/printer/printer` | 已绑定设备卡、在线状态、参数设置、测试/查询/解绑、未绑定空态、绑定弹层 | 未绑定、已绑定、在线/离线/未知、查询中、测试打印、解绑、SN/Key/名称输入、绑定成功/失败、浓度/速度/音量设置成功/失败 |

管理员端扩展原则：共享同一品牌色、字体、圆角、图标和状态语言，但保持工具型界面密度。禁止把迎宾页的大图片、超大标题和装饰卡片复制到管理页面。

## 11. 公共组件实施计划

| 模块 | 实施内容 | 约束 |
| --- | --- | --- |
| `app.wxss` | 替换颜色、文字、圆角、间距、阴影、控件高度和安全区令牌 | 只保留真正跨页面的规则；页面特例留在页面内 |
| `custom-tab-bar` | 按原型重建结构和本地图标，保持四条真实路由 | 不保留旧 marker、图标底座和缩放选中态 |
| `aurora-navbar` | 统一返回、标题、品牌/浅色变体和胶囊避让 | 所有非 Tab 页面复用，不自行绘制另一套返回按钮 |
| `avatarNicknameModal` | 统一白色实底、酒红操作、关闭、授权与键盘状态 | 未授权不得显示伪造身份 |
| 空/错/加载状态 | 提炼为 `aurora-state` 仅在三页以上重复后建立 | 支持图标、标题、说明和可选操作；不为单页建立抽象 |
| Vant 使用处 | 统一 `van-empty`、`van-tag`、`van-tabs`、`van-icon` 的颜色和间距，必要时用项目组件替换无法覆盖的空态 | 不修改 vendored Vant 源码 |
| ColorUI 使用处 | 保留图标能力，清除旧渐变和主题色泄漏 | 不继续扩大 ColorUI 布局类依赖 |

现有各页弹层业务差异较大，本轮只统一视觉合同和交互规则，不强行抽象成通用弹层组件，避免扩大回归面。

## 12. 实施阶段

### 阶段 0：分支、基线和资料固化

目标：确保后续改动可追溯、可比较、不会污染 `main`。

任务：

- 创建 `feature/aurora-brand-ui`。
- 记录当前提交、工作区和 15 个路由清单。
- 将确认后的原型 HTML、计划和必要参考截图选择性归档。
- 保存现有 15 页与已打开弹层的“改造前”截图索引。
- 确认 `.playwright-cli/`、临时 `output/` 和运行时假数据不进入提交。

验证：分支正确，`main` 未新增实施改动，参考文件可从计划路径打开。

### 阶段 1：未展示页面的视觉扩展确认

目标：不重新选择视觉方向，只确认已批准风格如何应用到原型未展示的页面类型。

需要提供一组接近最终效果的代表性稿件：

1. 充值页：余额、方案卡和固定操作栏。
2. 订单页：分段、点餐订单和充值订单。
3. 个人中心：未授权与已授权状态。
4. 结算/详情：表单、选项、固定操作栏。
5. 管理员首页：工具型入口和导航。
6. 管理员列表/表单/弹层：以菜品管理和店铺设置为代表。

确认内容：颜色、字体角色、页面密度、圆角层级、按钮、表单、状态、管理员端克制度。用户确认这组扩展稿后才进入对应页面实施；不再发散新的视觉方向。

### 阶段 2：设计令牌、资源和公共组件

涉及：

- `miniprogram/app.json`
- `miniprogram/app.wxss`
- `miniprogram/custom-tab-bar/*`
- `miniprogram/components/aurora-navbar/*`
- `miniprogram/components/avatarNicknameModal/*`
- 必要的本地图标和迎宾背景资源

任务：

- 建立本计划第 6 节的语义令牌。
- 输出并接入原型底部导航图标和上滑提示图标。
- 完成原型式 TabBar，并接通四个真实路由。
- 统一导航、授权弹层、空错状态基础形态。
- 建立固定栏和安全区计算规则。
- 不在此阶段批量改页面布局。

验证：四个 Tab 切换、选中状态、原生胶囊、返回链路、授权弹层和安全区在 320/390/430px 宽度下通过。

### 阶段 3：首页技术验证与迎宾动效

涉及：

- `miniprogram/pages/index/index.{json,wxml,wxss,js}`
- `miniprogram/pages/index/welcome-motion.wxs`
- 欢迎背景和图标资源

顺序：

1. 先完成最小 WXS 技术验证，证明滚动、分类同步、分页、刷新和下拉恢复可共存。
2. 实现展开态，与 430/390/320 原型截图逐项对照。
3. 实现 0、0.25、0.5、0.75、1 五个进度关键状态。
4. 实现锁存、顶部下拉、0.45 吸附和反向恢复。
5. 实现会话首次展开、Tab/详情/结算返回恢复和生命周期清理。
6. 接回真实分类、菜品、分页和购物车数据。
7. 统一购物车栏、抽屉、规格弹层和全部状态。

阶段门槛：只要动效不跟手、恢复不连续、分类跳转闪动、菜单交界不精确或背景失败导致空白，本阶段即未通过，不进入顾客端批量迁移。

### 阶段 4：顾客端其余页面

顺序按主流程和共享影响执行：

1. `dish-detail`：确定详情、规格、数量和固定操作栏规范。
2. `settle`：确定表单、支付方式和提交栏规范。
3. `recharge`：应用余额、方案和固定栏规范。
4. `myorder`：应用分段、订单卡和状态规范。
5. `myhome`：应用身份、服务入口和授权规范。

每页完成后验证正常、空、加载、错误、未授权、图片失败、最长文案、键盘和固定栏。四个 Tab 页还需验证底部导航和页面状态隔离。

### 阶段 5：管理员端

顺序按公共形态复用执行：

1. 管理员首页和统一导航。
2. 店铺设置，先确定表单与保存栏。
3. 菜品分类、会员和充值方案，确定列表与编辑弹层。
4. 菜品管理，覆盖复杂大弹层、规格、图片和嵌套标签编辑。
5. 订单管理，覆盖筛选、自动刷新和高密度订单。
6. 桌码管理，覆盖二维码、Painter、权限和下载。
7. 打印机管理，覆盖设备状态和参数控制。

每个页面所有现有可见控件都要真实点击，不能只截图首页状态。

### 阶段 6：状态补齐、性能和可访问性

任务：

- 全局扫描旧亮红、橙红渐变、旧圆角和无语义硬编码颜色。
- 补齐异步页面的可见加载、空、错误、重试和提交状态。
- 检查最长数据、字体放大、图片失败、慢网和分页追加。
- 检查所有热区、对比度、图标语义、按压反馈和禁用反馈。
- 检查弹层滚动锁、键盘、关闭、返回和临时状态清理。
- 检查迎宾 WXS、自动刷新、页面隐藏和定时器清理。
- 检查包体积，迎宾资源不得造成主包不可接受增长。

### 阶段 7：全量真实渲染验收和收尾

任务：

- 使用 `speedcode-skill` 驱动微信开发者工具逐路由截图和交互。
- 对迎宾展开、半收起、收起、下拉恢复录屏并逐帧检查。
- 在同一 430 x 844 状态下并排对照原型和小程序实现。
- 运行全部静态检查、页面流程和边界状态。
- 修复发现的问题后重新执行受影响范围；公共组件变化重新执行全量。
- 从最终实现生成 `DESIGN.md`，记录实际落地的设计系统。
- 更新本计划执行记录和 `UI_IMPLEMENTATION_PLAN.md` 的最终验收状态。

## 13. 状态与数据边界矩阵

### 13.1 全局状态

每个适用页面都必须验证：

| 状态 | 验收要求 |
| --- | --- |
| 首次加载 | 页面骨架或稳定加载态属于当前页面，不闪现其他页面 |
| 正常 | 使用真实内容密度，主要操作可见可用 |
| 空 | 说明当前为空，必要时提供正确下一步 |
| 错误 | 错误可见、不遮挡内容，有可执行重试或返回 |
| 慢网 | 尺寸稳定，不因后到图片或数据跳动 |
| 图片失败 | 同尺寸占位，不显示破图图标或塌陷 |
| 未授权 | 明确显示未授权，不伪造身份 |
| 禁用 | 视觉、文案和点击行为一致，不能仍触发提交 |
| 提交中 | 防重复提交，按钮尺寸不变，当前字段可见 |
| 成功 | 只在真实业务成功后显示，并到达正确后续状态 |
| 失败/取消 | 不借用成功样式，保留重试或退出路径 |

### 13.2 边界数据

- 店名：空、正常、20 个中文字符。
- 欢迎词：空、正常、48 个中文字符。
- 桌号：空、普通数字、含中文、最长 64 字符；紧凑卡和购物车栏必须截断，详情处可完整查看。
- 分类：0、1、20 个；短名称和最长合理名称。
- 菜品：0、1、20、40+；24 字名称、80 字描述、0/1/9 张图。
- 规格：单规格、多规格、全部停用；长规格名和长价格。
- 标签：无标签、单选、多选、必选未选、多标签换行。
- 购物车：0、1、99、100+ 件；`0.00`、普通价格、`999999.99`。
- 备注：空、120 字、换行和中英文混排。
- 订单：点餐、充值、长订单号、多商品、多标签、不同状态。
- 充值：无赠送、有赠送、推荐、停用、大金额。
- 用户：未授权、无头像、无昵称、无手机号、完整授权、大余额。
- 管理列表：0、1、20、40+，分页加载和重复刷新。
- 二维码/打印机：资源加载中、失败、权限拒绝、离线和未知状态。

测试数据不得使用仿真个人昵称、头像或完整手机号。演示身份统一使用明确的未授权状态或非个人化测试标识。

## 14. 真实渲染与交互验收

### 14.1 工具和前置条件

必须使用：`/home/ao/project/mini-program/speedcode-skill`。

前置条件：

- 微信开发者工具可以打开项目。
- `设置 -> 安全设置 -> 服务端口` 已开启。
- Node.js、npm 和 `miniprogram-automator` 可用。
- 使用有效 AppID/云环境执行真实数据、支付、扫码和管理提交验收。

准备命令：

```bash
/home/ao/project/mini-program/speedcode-skill/speedcode-skill/scripts/ensure-wechat-automator.sh
```

建立本项目专用 QA 配置，至少包含：

- 15 个路由的全量截图流。
- 四个 Tab 的切换、选中和快速切换流。
- 购物车、规格、授权、管理员密码、分类、菜品、余额、充值方案、桌码、打印机等打开态。
- 顾客主流程和管理员主要 CRUD 流程。

中间输出写入：

```text
/tmp/aurora-suite-brand-ui-vqa/<run-id>/
```

最终通过证据选择性归档到：

```text
docs/visual-qa/<date>/brand-ui-final/
```

### 14.2 代表性设备

至少覆盖：

| 类别 | 建议视口 | 重点 |
| --- | --- | --- |
| 小屏 | 320px 宽 | 分类宽度、菜品卡、价格按钮、表单和弹层 |
| 标准 | 390 x 844 | 当前项目基线、键盘和固定栏 |
| 大屏 | 430px 以上 | 与 430 x 844 原型同状态对照 |
| 短屏 | 375 x 667 附近 | 首屏露出、弹层、固定操作栏 |
| 安全区设备 | 带底部手势区 | TabBar、购物车、充值栏和提交栏 |

同时覆盖不同状态栏高度、刘海/圆角屏、原生胶囊位置和系统字体放大。

### 14.3 迎宾专项

实际手势并录屏验证：

1. 冷启动首次进入完整展开。
2. 慢速上滑经过 0.25/0.5/0.75 进度。
3. 快速上滑吸附到收起态。
4. 中途反向滑动不跳变。
5. 菜品列表继续滚动和分页。
6. 回到顶部但不下拉时保持收起。
7. 下拉不足阈值回到收起。
8. 下拉超过阈值完整展开。
9. 展开后继续下拉触发刷新且不破坏动效。
10. 分类点击、规格弹层、购物车抽屉、Tab 切换、详情返回和结算返回。
11. 页面进入后台再返回。
12. 图片慢加载和失败降级。

逐帧检查背景、文字、信息卡、菜单圆角、间隙、提示、购物车和 TabBar，不得出现一帧错位或其他页面闪现。

### 14.4 控件专项

- 底部四个导航入口全部点击并核对路由与选中态。
- 所有非 Tab 页面返回按钮点击并核对去向。
- 所有添加、减少、清空、结算、充值、提交、取消订单和分享入口。
- 所有分段、单选、多选、开关、步进器和筛选。
- 所有输入框、文本域、键盘切换、校验和提交。
- 所有弹层、抽屉、遮罩、关闭、返回和背景滚动锁。
- 所有管理入口和新增、编辑、删除、启停、搜索、刷新、预览、下载、扫码、打印。

截图和 JSON 状态不能替代真实点击；每个操作必须验证视觉反馈和实际结果。

### 14.5 启动与页面隔离

- 冷启动、热启动、首次进入、重复进入。
- Tab 快速切换、详情前进/返回、结算前进/返回。
- 管理员页面连续前进/返回。
- 刷新、后台/前台、数据重新加载。
- 不得闪现上一页面、错误方案、历史内容、未初始化布局或错误选中 Tab，即使只出现一帧也不通过。

## 15. 静态与工程检查

每个阶段按影响范围执行：

```bash
git diff --check
```

并完成：

- 所有修改 JS 文件执行 `node --check`。
- 所有 JSON 文件执行解析检查。
- 微信开发者工具完整编译，无 WXML、WXSS、WXS 和组件引用错误。
- 扫描旧 `#d94335`、`#f36a5d`、橙红渐变及页面级重复 token，逐项判断并清理。
- 扫描未绑定事件、无效路由、失效图片路径和未清理计时器。
- 检查主包资源体积和迎宾背景解码成本。
- 检查控制台错误、警告、网络失败和资源加载失败。

静态检查只能证明语法和引用，不能替代真实渲染。

## 16. 风险与处理

| 风险 | 影响 | 处理 |
| --- | --- | --- |
| WXS 与现有 `onGoodsScroll` 冲突 | 分类同步或动效卡顿 | 阶段 3 先做最小技术验证；WXS 负责高频样式，逻辑层调用节流 |
| 下拉恢复与原生刷新冲突 | 无法恢复或无法刷新 | 明确手势优先级，必要时迁移为内层 refresher，并真实手势验证 |
| 原生胶囊和不同状态栏 | 欢迎文案或紧凑卡遮挡 | 所有顶部尺寸由 `wx.getWindowInfo` 和胶囊矩形计算，不猜固定安全区 |
| TabBar、购物车和安全区叠加 | 菜品和按钮被遮挡 | 统一固定栏高度变量和内容底部占位，代表性安全区设备实测 |
| 全局样式污染 | 其他行业或管理页面串色 | 使用页面根类和稳定组件边界，减少无范围全局选择器 |
| 旧 ColorUI/Vant 颜色泄漏 | 页面出现亮红或不同圆角 | 扫描使用处并局部覆盖，不修改第三方源码 |
| 背景图片热链或过大 | 首屏空白、慢加载、包体积过大 | 使用自有资源、压缩尺寸、预留深色降级并测包体积 |
| 动态店名/欢迎词过长 | 迎宾和紧凑卡溢出 | 使用现有 20/48 字约束，最长文案实测，紧凑态明确截断 |
| 桌号允许 64 字符 | 信息卡和购物车挤压 | 紧凑区域单行省略，完整值在合适页面展示，最长值实测 |
| 页面弹层多且结构不同 | 通用重构引发回归 | 统一样式合同，不强制抽象业务弹层 |
| AppID/云环境为占位值 | 无法验证支付、扫码、打印和真实 CRUD | 配置有效测试环境后才能最终判定通过；未配置时必须结论为未通过 |
| 未跟踪文件较多 | 误提交或误删除用户内容 | 不清理工作区，不使用 `git add .`，逐文件暂存和检查 |

## 17. 完成定义

只有同时满足以下条件，任务才完成：

- 正式实施全部位于 `feature/aurora-brand-ui`。
- 已确认迎宾原型在小程序中真实实现，不是静态截图或两个页面切换。
- 上滑收起、菜单滚动、顶部下拉和完整恢复连续、跟手、可逆。
- 原型菜单间隙、圆角、双箭头提示、购物车栏和底部导航准确落地。
- 四个底部导航入口外观与原型一致，功能、路由和选中状态正确。
- 15 个路由全部完成风格、色调、控件、状态和安全区统一。
- 顾客端与管理员端保持同一设计语言，同时符合各自操作密度。
- 现有业务功能、数据、路由、支付、授权和管理能力无回归。
- 正常、加载、空、错误、未授权、禁用、提交、图片失败和边界数据均验证。
- 小屏、标准、大屏、短屏、状态栏、胶囊、字体和底部安全区均验证。
- 所有可见控件和关键流程已经真实操作，不是只看代码或截图。
- 所有截图逐张打开检查，迎宾动效录屏逐帧检查。
- 静态检查、开发者工具编译、控制台和资源检查无问题。
- 最终 `DESIGN.md`、实施记录和 QA 证据已归档。
- 没有环境阻塞、遗留缺陷或未验证项。

## 18. 执行跟踪

| 阶段 | 状态 | 进入条件 | 退出条件 |
| --- | --- | --- | --- |
| 0 分支与基线 | 已完成 | 本计划确认 | 新分支、参考和前置状态明确 |
| 1 未展示页面视觉扩展 | 已完成 | 原型已确认 | 顾客端和管理员端代表性页面视觉规则固化至 `DESIGN.md` |
| 2 设计令牌与公共组件 | 已完成 | 阶段 1 通过 | TabBar、Navbar、授权弹层和令牌完成真实渲染检查 |
| 3 首页与迎宾动效 | 已完成 | 阶段 2 通过 | 展开、收起、恢复、菜单、购物车和弹层冻结通过 |
| 4 顾客端其余页面 | 已完成 | 阶段 3 通过 | 6 个顾客路由标准态、空态、错误态和关键操作通过 |
| 5 管理员端 | 已完成 | 阶段 4 通过 | 9 个管理路由标准态和代表性弹层通过 |
| 6 状态与质量加固 | 已完成 | 页面迁移完成 | 旧亮红清理、短屏适配、Tab 锁定和桌码成功态完成 |
| 7 全量真实验收 | 已完成（临时环境） | 临时 AppID 与模拟数据可用 | 15 路由截图、迎宾关键帧、交互专项和静态检查完成；真实云支付/CRUD 不在本地验收范围 |

### 18.1 2026-08-09 最终验收记录

- 最终标准截图流：`docs/visual-qa/2026-08-09/brand-ui-final/full-routes/summary.json`，15 个路由结果均为 `ok: true`。
- 迎宾关键帧和代表性尺寸：`docs/visual-qa/2026-08-09/brand-ui-final/motion/`、`interaction/`；覆盖展开、连续上滑、锁存、短/长下拉、320×568 和 430×932。
- 规格弹层和购物车抽屉通过运行中的页面真实方法触发，确认遮罩、背景冻结、TabBar 锁定/恢复、规格/辣度/数量和加购后的金额更新。
- 桌码管理的本地二维码和海报成功态已渲染，操作入口显示预览、下载和删除；真实下载权限与云端生成仍需部署环境复验。
- `git diff --check`、业务 JS、关键 WXS、JSON 解析和 15 路由资源检查通过；页面级旧亮红和装饰性斜纹已清理。
- 临时 AppID 与本地模拟数据范围内的结论：`全部检查通过`。真实支付、云端 CRUD、微信授权和打印机网络结果不在临时环境验收范围。
